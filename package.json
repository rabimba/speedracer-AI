{
  "name": "trustable-ai-codelab",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "koru-application"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "artifacts:pixel:capture": "node scripts/capture-pixel-demo-artifacts.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
